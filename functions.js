(function(){
console.log("function called ...");
}());
 // Immediately called/invoked function expressions no-param
 (function(name){
 console.log(`function called with ${name}...`);
 }('steve jobs'));
 // Immediately called/invoked function expressions one-param
